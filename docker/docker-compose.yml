version: '3'
     
  services:
     nginx_img:
            container_name: testing
            image: nginx:1.13.8
            volumes:
                - ..:/usr/share/nginx/html
                - ../logs/nginx:/var/log/nginx
            ports:
                - 1000:80
